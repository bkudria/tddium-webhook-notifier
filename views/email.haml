= precede 'Build: ' do
  %a{href: "https://api.tddium.com/reports/#{session}"}> ##{session}
%br
= precede 'Commit: ' do
  %a{href: "https://github.com/#{[org, repo].compact.join('/')}/commit/#{commit}"}>= commit


- if debug
  %p
    \--
  %br
  %small
    This email triggered by a webhook with the following payload:
    %pre= payload



